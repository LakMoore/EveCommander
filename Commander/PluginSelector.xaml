<Window x:Class="Commander.PluginSelector"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Commander"
        mc:Ignorable="d"
        Title="PluginSelector" Height="230" Width="300"
        Background="{DynamicResource BackgroundBrush}" Foreground="{DynamicResource ForegroundBrush}">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <TextBlock Text="Select plugins to enable:" Margin="10,10,10,10" Grid.Row="0" />
    <ListBox x:Name="PluginList" ItemsSource="{Binding Items}" SelectionMode="Multiple" ScrollViewer.VerticalScrollBarVisibility="Auto" Grid.Row="1">
      <ListBox.ItemContainerStyle>
        <Style TargetType="ListBoxItem">
          <Setter Property="HorizontalContentAlignment" Value="Stretch" />
          <Setter Property="Background" Value="Transparent" />
          <EventSetter Event="PreviewMouseLeftButtonDown" Handler="ListBoxItem_PreviewMouseLeftButtonDown" />
        </Style>
      </ListBox.ItemContainerStyle>
      <ListBox.ItemTemplate>
        <DataTemplate>
          <!-- row content: checkbox + label; clicks automatically toggle IsSelected on the container -->
          <DockPanel Background="Transparent" Cursor="Hand">
            <CheckBox
                IsChecked="{Binding IsSelected, Mode=TwoWay}"
                DockPanel.Dock="Left"/>
            <TextBlock Text="{Binding Name}" Margin="6,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False" />
          </DockPanel>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>
    <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="10">
      <Button Margin="0,0,10,0" Padding="5" Click="Button_Click" Style="{DynamicResource {x:Type Button}}">OK</Button>
      <Button Padding="5" Click="Cancel_Click" Style="{DynamicResource {x:Type Button}}">Cancel</Button>
    </StackPanel>
  </Grid>
</Window>
