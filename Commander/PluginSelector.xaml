<Window x:Class="Commander.PluginSelector"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Commander"
        mc:Ignorable="d"
        Title="PluginSelector" Height="230" Width="300">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <TextBlock Text="Select plugins to enable:" Margin="10,10,10,10" Grid.Row="0" />
    <ListBox x:Name="PluginList" SelectionMode="Multiple" ScrollViewer.VerticalScrollBarVisibility="Auto" Grid.Row="1" >
      <ListBox.ItemTemplate>
        <DataTemplate>
          <DockPanel>
            <CheckBox
                IsChecked="{Binding IsSelected, Mode=TwoWay}"
                DockPanel.Dock="Left"/>
            <TextBlock Text="{Binding Name}" Margin="6,0,0,0" VerticalAlignment="Center" />
          </DockPanel>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>
    <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="10">
      <Button Margin="0,0,10,0" Padding="5" Click="Button_Click">OK</Button>
      <Button Padding="5" Click="Cancel_Click">Cancel</Button>
    </StackPanel>
  </Grid>
</Window>
